<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Активное состояние (кнопка включена) - синий -->
    <item android:state_enabled="true" android:color="@color/button_active" />
    <!-- Неактивное состояние (кнопка выключена) - серый -->
    <item android:state_enabled="false" android:color="@color/button_disabled" />
</selector>